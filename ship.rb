class Ship
  attr_accessor :length

  def initialize(length)
    @length = length
  end

  def place(x, y, horizontal)

    @length.times { |spot| }
  end

  def covers?(x, y)
    @location.include?([x, y])
  end

  def overlaps_with?

  end

  def fire_at

  end
end
